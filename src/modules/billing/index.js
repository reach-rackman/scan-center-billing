import { Typography } from '@material-ui/core';
import PageHeader from '../common/PageHeader';

function Billing() {
    return (
        <PageHeader pageName="Billing Details" />
    )
};

export default Billing;